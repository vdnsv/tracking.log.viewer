apply plugin: 'java'
apply plugin: 'application'

version '1.0-SNAPSHOT'

sourceCompatibility = JavaVersion.VERSION_1_8
targetCompatibility = JavaVersion.VERSION_1_8

buildDir = 'build-gradle'
mainClassName = 'by.ep.util.trackviewer.ui.TrackViewer'

dependencies {

    compile zipTree("swt/swt-4.7.1a-win32-win32-x86_64.zip").filter { f -> f.name == 'swt.jar'}
}

